{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "recipe:fastapi",
            "type": "debugpy",
            "request": "launch",
            "module": "src.main",
            "cwd": "${workspaceFolder}/recipes/fastapi",
            "console": "integratedTerminal",
            "env": {
                "PYTHONPATH": "${workspaceFolder}/recipes/fastapi;${workspaceFolder}"
            }
        }
    ]
}